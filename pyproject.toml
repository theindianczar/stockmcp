[project]
name = "stockmcp"
version = "0.1.0"
description = "Investing app skeleton"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"
